function reverse(str) {
    let inverser = "";
  
    for (let i = string.length - 1; i >= 0; i--) {
      inverser += string[i];
    }
  
    return inverser;
  }

  console.log(reverse("Arthur"));

